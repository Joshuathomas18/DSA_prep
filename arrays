#Contains duplicate 
approach:
  1. u can traverse for each no. o(N^3)
  2. u can sort list and traverse once0(nlogn+n)
  3.create a set if anything same as in set return true o(1)

def contains_dupl(self,nums):
  h=set()
  for num in nums:
    if num in h:
      return true
    else:
      h.append(num)
  return false


def contains_dupl(self,nums):
    nums.sort()
    for i in nums:
      if nums[i]==nums[i-1]:
        return True
    return False

#Vlid anagram

approach:
1. see lengths are same
2. make 2 dict
3.add all elements to tht dict
4. if dict same then true else false

def Valid_anagram(self,s,t):
  if length(s) != length(t):



